package Basic;

public class Lab019 {
    public static void main(String[] args) {

        // Java swich expression must be of yte, short, int, long,with its Wrapper type),
        //enums and String. Beginning with JDK7,
        // it also works with enumerable types(Enums in java the String class and Wrapper classes


        int a = 10;
        switch(a){

        }
        char ch = 'A';
        switch(ch){

        }

        float f = 30.0f;
        switch((int) f){ // Narro - type casting Explicit

        }
        double d = 30.0;
        switch((int) d){

        }
       // boolean b = true;
        //switch(b) {  }

        long all = 30l;
        switch((int) all){  }

    }
}
